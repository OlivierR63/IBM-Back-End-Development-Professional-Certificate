# ===============================================
# DOCKER IGNORE FILE FOR MICROSERVICES ARCHITECTURE
# Prevents unnecessary files from being included in the build context sent to the Docker daemon.
# ===============================================

# 1. GIT/SYSTEM FILES
# These files are used locally and should never be copied into the image.
.git
.gitignore
.gitmodules
.DS_Store

# 2. LOCAL ENVIRONMENT & CACHE FILES
# These are generated locally and should be installed/created *inside* the container
# during the build process, never copied from the host.
.env
.venv
env/
venv/
__pycache__/
*.pyc
lib/
lib64/

# 3. PYTHON/NODE.JS DEPENDENCY FOLDERS
# Node.js dependencies (Songs service)
# If your Dockerfile runs 'npm install', ignore the local node_modules folder.
node_modules
npm-debug.log

# Python dependencies (Capstone, Pictures, Songs services)
# If your Dockerfile runs 'pip install', ignore any local dependency directories.
pip-wheel-metadata/
build/
dist/
.pytest_cache/
.tox/

# 4. APPLICATION SPECIFIC FILES TO IGNORE
# Files that are generated by the app or contain local data.
# The database file is created by Capstone and shouldn't be included in *other* images.
db.sqlite3
*.log

# 5. IDE/TOOLS
# Files specific to development tools
.idea/
.vs/
.vscode/